import 'package:cc_core/cc_core.dart';

/// The free tier: five courses in the ledger, forever. Only adding a
/// sixth is ever gated (Phase C wires the gate and paywall).
const courseFreeLimit = FreeLimit(5, 'courses');
